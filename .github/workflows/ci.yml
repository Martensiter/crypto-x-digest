name: CI
on:
  push:
    branches-ignore: ['wip/checkpoint/**']
  pull_request:
jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v6
        with:
          python-version: '3.11'
      - run: python -m compileall -q scripts
  notify-failure:
    needs: ci
    if: failure()
    uses: ./.github/workflows/notify-discord-reusable.yml
    with:
      channel: alerts
      custom_text: "❌ CI failed on `${{ github.ref_name }}` — ${{ github.repository }}"
    secrets: inherit
